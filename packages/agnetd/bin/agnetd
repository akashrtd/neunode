#!/usr/bin/env node
"use strict";

const { binPath } = require("../lib/main");
const child_process = require("child_process");

const result = child_process.spawnSync(binPath, process.argv.slice(2), {
  stdio: "inherit",
  windowsHide: true,
});

process.exitCode = result.status ?? 1;
