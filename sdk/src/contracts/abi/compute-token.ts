import { neunodeTokenAbi } from "./neunode-token.js";

/**
 * ComputeToken ABI — nCompute (GPU/CPU hours).
 * Identical to NeunodeToken ABI; constructor sets name/symbol/decimals.
 * Source: contracts/src/tokens/ComputeToken.sol
 */
export const computeTokenAbi = neunodeTokenAbi;
